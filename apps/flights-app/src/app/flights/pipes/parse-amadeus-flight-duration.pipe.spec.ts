import { ParseAmadeusFlightDurationPipe } from './parse-amadeus-flight-duration.pipe';

describe('ParseAmadeusFlightDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseAmadeusFlightDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
